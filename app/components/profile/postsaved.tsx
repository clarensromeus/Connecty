import * as React from "react";
import { Text } from "react-native-paper";
import { View } from "react-native";

export interface IPostSavedProps {}

export function PostSaved(props: IPostSavedProps) {
  return (
    <View>
      <View></View>
      <View></View>
    </View>
  );
}
